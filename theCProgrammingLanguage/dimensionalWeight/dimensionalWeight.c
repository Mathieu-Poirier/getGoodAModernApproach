#include <stdio.h>

int main(void) {
  int exampleNumber = 123;  // integer type
  float exampleFloat = 0.1; // floating point type
  printf("Look at this %d, %f\n", exampleNumber, exampleFloat);
  return 0;
}